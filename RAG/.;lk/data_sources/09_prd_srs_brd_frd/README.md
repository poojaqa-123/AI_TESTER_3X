# 09 — PRD / SRS / BRD / FRD

Drop requirement docs here as `.pdf`, then ingest from the **Sources** tab, or:

```bash
python -m ingestion.documents --path data_sources/09_prd_srs_brd_frd --source-type doc
```

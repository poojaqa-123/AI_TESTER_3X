flask>=3.0
python-dotenv>=1.0
pandas>=2.2
openpyxl>=3.1
qdrant-client>=1.10
FlagEmbedding>=1.3
# Pinned to the last 4.x release: FlagEmbedding 1.4.0 straddles a breaking
# transformers boundary — its embedder path (BGEM3FlagModel) needs the newer
# `dtype=` from_pretrained kwarg, while its reranker path (FlagReranker)
# still calls tokenizer.prepare_for_model(), which transformers 5.x removed.
# 4.57.6 is the last release with both.
transformers==4.57.6
accelerate==0.34.2
sentence-transformers==3.3.1
openai>=1.40
torch>=2.2

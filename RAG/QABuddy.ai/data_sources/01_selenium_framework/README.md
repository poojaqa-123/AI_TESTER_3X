# 01 — Selenium framework

Source: https://github.com/PramodDutta/ATB13xSeleniumAdvanceFramework

Clone or copy the framework repo into this folder (or a subfolder), then run ingestion
from the **Sources** tab, or:

```bash
python -m ingestion.code_repo --path data_sources/01_selenium_framework --source-name selenium_framework --language java
```

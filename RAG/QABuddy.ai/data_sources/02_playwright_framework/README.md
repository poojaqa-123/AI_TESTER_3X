# 02 — Playwright framework

Source: https://github.com/PramodDutta/Advance-Playwright-Framework

Clone or copy the framework repo into this folder (or a subfolder), then run ingestion
from the **Sources** tab, or:

```bash
python -m ingestion.code_repo --path data_sources/02_playwright_framework --source-name playwright_framework --language ts
```

# 08 — Lucid charts (exported to text)

Export each Lucid chart's text/labels to a `.txt` file and drop it here, then
ingest from the **Sources** tab, or:

```bash
python -m ingestion.lucid_charts --path data_sources/08_lucid_charts
```

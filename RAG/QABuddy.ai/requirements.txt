flask>=3.0
gunicorn>=22.0
python-dotenv>=1.0
pandas>=2.2
openpyxl>=3.1
qdrant-client>=1.10,<1.12  # keep in step with docker-compose.yml's qdrant:v1.11.0 server
pypdf>=4.2
FlagEmbedding>=1.3
# Pinned below transformers 5.x: FlagReranker still calls
# tokenizer.prepare_for_model(), which transformers 5.x removed. Verified
# working combo (embed + rerank + generation, end-to-end): FlagEmbedding
# 1.4.0 + transformers 4.57.6.
transformers==4.57.6
accelerate==0.34.2
sentence-transformers==3.3.1
openai>=1.40
# torch<2.6 refuses to torch.load() non-safetensors checkpoints (CVE-2025-32434
# guard) — bge-m3/bge-reranker pull some non-safetensors files, so pin >=2.6.
torch>=2.6
